/**
 * @name helloWorld
 * @param { string } name This is the username that you will want to say hello to
 * @returns { string } This is the string gretting the user.
 */
export const helloWorld = (name: string) => `Hello, ${name}`;

/**
 * @name helloWorld
 * @param { string } name This is the username that you will want to say Goodbye to
 * @returns { string } This is the string saying goodybye the user.
 */
export const goodByeWorld = (name: string) => `Goodybye, ${name}`;
